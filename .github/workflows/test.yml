name: Test

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  fixtures:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - fixture: react-task-board
            build: true
          - fixture: typescript-order-api
            build: false
          - fixture: vite-reading-list
            build: true
    defaults:
      run:
        working-directory: fixtures/${{ matrix.fixture }}/environment
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: fixtures/${{ matrix.fixture }}/environment/package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm run typecheck
      - if: matrix.build
        run: npm run build
