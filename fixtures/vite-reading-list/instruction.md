# Improve the reading list

Refactor this small Vite application to improve its code quality while preserving its behavior.

The application must continue to:

- start with one read and one unread article;
- add and persist articles in `localStorage`;
- filter all, read, and unread articles;
- toggle articles and clear read articles; and
- show accurate unread and total counts.

You may reorganize `index.html` and files under `src`. Keep the implementation proportionate to this small product. Do not change tests, dependencies, package metadata, or configuration, and do not add product behavior.

Run the repository checks before finishing.
