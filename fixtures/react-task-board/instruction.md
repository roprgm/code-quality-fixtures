# Improve the task board

Refactor this small React application to improve its code quality while preserving its behavior.

The board must continue to:

- start with one open and one completed task;
- add and persist tasks in `localStorage`;
- filter all, open, and completed tasks;
- toggle tasks and clear completed tasks; and
- show accurate open and completed counts.

You may reorganize files under `src`. Keep the implementation proportionate to this small product. Do not change tests, dependencies, package metadata, or configuration, and do not add product behavior.

Run the repository checks before finishing.
